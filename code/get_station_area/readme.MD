Before running the code, please install the required dependencies:

```
pip install -r requirements.txt
```


use get_iso_nyc.py or get_iso_chi.py to get the ischrone-based station areas for different cities.