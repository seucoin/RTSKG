Before running the code, please install the required dependencies:

```
pip install -r requirements.txt
```


For each city, replace your Mapbox access token in isochrone_mapbox.py and use get_iso_nyc.py or get_iso_chi.py in the corresponding directory to obtain the station areas.